// external js: flickity.pkgd.js, fullscreen.js

function noSpam() {
    // Get the text field
    var emailOne = "alys";
    var emailTwo = "otterwr";
    var emailThree = "gmail.com";

     // Copy the text inside the text field
    navigator.clipboard.writeText(emailOne+"sap"+emailTwo+"ites@"+emailThree);
  
    // Alert the copied text
    alert("Email Copied" + copyText.value);
  } 